language: 'typescript'

tasks:
    build:
        command: 'tsc'
    start:
        command: 'node dist/index.js'
    dev:
        command: 'tsx watch index.ts'
        env:
            NODE_ENV: 'development'
        local: true
